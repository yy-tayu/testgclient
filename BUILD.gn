# import("third_party/webrtc/webrtc.gni")
import ("build_overrides/build_overrides.gni")
group("default") {
    deps = ["//src:test_shared"]
    if(native_testing)
    {
        deps += [ "//src:test_gn" ]
    }
}
